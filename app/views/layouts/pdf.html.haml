!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:charset => "utf-8"}/
    :css
      body {
        font-family: Arial, Verdana;
        font-size: 9pt;
      }

      .align-center {
        text-align: center;
      }

      .align-left {
        text-align: left;
      }

      .align-right {
        text-align: right;
      }

      .header {
        margin-bottom: 20px;
      }

      .left {
        float: left;
      }

      .right {
        float: right;
      }

      table.header {
        width: 100%;
        border-collapse: collapse;
      }
      table.header th, table.header td {
        padding: 4px;
      }

      table.table-report {
        width: 100%;
        border-collapse: collapse;
      }
      table.table-report tr th {
        padding: 8px 0;
        border: thin solid #000;
      }
      table.table-report tr td {
        border: thin solid #000;
        padding: 5px;
        vertical-align: top;
        box-decoration-break: clone;
      }
      table.table-report tr td ol {
        padding-left: 20px;
        margin-top: 0;
      }
      .center {
        margin: auto;
        width: 80%;
        padding: 10px;
      }
      .button-actions li {
        display: inline-block;
        padding: 5px;
      }
      a.button {
        -webkit-appearance: button;
        -moz-appearance: button;
        appearance: button;

        text-decoration: none;
        color: initial;
        padding: 5px;
      }

  %body
    %div{:class => ("center" unless params[:pdf])}= yield
